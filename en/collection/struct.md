# Struct

```
Point := (
    x: float, 
    y: float
)

O = Point(
    x = 0.0,
    y = 0.0
)

I = Point(
    x = 1.0,
    y = 1.0
)

Person(name, addr) <: (Eq, Add, Mul) := {
    name :: {
    
    }
    
    Eq :: {
    
    }
    
    Add :: {
    
    }
    
    #`$? + {x}` : {
        
    }
    
    #`- $?`: {
    
    }
}
```
